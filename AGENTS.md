# AI PMO 项目协作规范

## 项目定位与入口

本仓库以能源行业合同全生命周期管理系统为案例，维护分目录的项目管理工作簿、参考资料和 Python 自动化程序。开始工作前先读本文件和 [ROADMAP.md](ROADMAP.md)；涉及运行方式、目录或产品能力时再读 [README.md](README.md) 和 [自动化说明](合同全生命周期管理系统-项目管理套件/automation/README.md)。本文件是本项目 Agent 协作规则，不是通用个人偏好。

## 目录与数据边界

- `合同全生命周期管理系统-项目管理套件/01_` 至 `11_`：按管理领域保存当前业务工作簿和交付文件；`90_项目数据中心/` 保存项目数据与联动规则。
- `合同全生命周期管理系统-项目管理套件/automation/`：扫描、校验、构建、同步、Agent 分析和报告生成程序；`tests/` 为回归测试。
- `合同全生命周期管理系统-项目管理套件/inputs/`：套件运行参考输入；`outputs/`：自动化输出。根目录的 `inputs/`、`outputs/` 是早期输入和成果，不与套件内目录混用。
- `_archive/`：历史版本，仅供追溯。不得把归档文件当作当前基线，也不得在未核实引用关系时删除。

人工确认的业务事实和审批结论以当前业务台账及其证据为准。`ai_pmo.db` 是自动化处理的结构化存储和运行快照，不因数据入库就取得业务审批效力；Agent JSON、周报、驾驶舱和简报属于派生分析。遇到工作簿、数据库和报告不一致时，先核对源文件、Sheet、记录 ID、数据日期与运行日志，再决定修正位置；不得静默覆盖人工填写或已确认的内容。

## 工作流

1. 确认任务范围、当前文件版本和 `git status`；只修改与任务直接相关的文件，保留用户已有改动。
2. 修改工作簿前核对列名、唯一编号、公式、合并单元格、筛选与冻结、数据验证和跨表依赖。计划、资源、成本、风险等联动修改须检查上下游口径。
3. 修改生成逻辑时先明确输入、输出及失败边界；优先复现问题并补充相关回归测试。同步或构建可能覆盖工作簿，运行前确认其目标文件和人工维护字段。
   命令级来源、写入和冲突处理见 [数据血缘与写入边界](合同全生命周期管理系统-项目管理套件/automation/docs/DATA_LINEAGE_AND_WRITE_POLICY.md)；尤其不得把 `build/run` 当作正式台账的日常刷新。
4. Agent 默认只读源台账。每项正式发现须能追溯来源文件、Sheet、记录 ID 和源行号；缺失证据时标记“待核实”，不得写成已确认事实。
5. 项目基线、预算、风险等级、任务或问题关闭、变更批准、采购选型及对外发布由授权人确认；自动化只能提出建议和生成待审材料，不代替审批。
6. 影响项目状态的实施完成后更新 `ROADMAP.md`；只读分析或临时诊断不制造进度记录。`README.md` 只写当前实际可用的能力和可运行命令，不把设想写成已实现。

## 验证与交付

- 自动化代码变更至少运行相关测试；跨模块改动运行 `automation/tests/` 全量测试，并核对生成文件的关键 Sheet、字段和记录数。
- Excel 变更按风险检查公式、样式、筛选、冻结和文件可打开性；“程序成功保存”不等于显示和交互已验证。
- 报告修改检查事实、推断、待确认事项和审批边界的标识是否清楚。
- 交付时说明改动文件、验证结果、未验证范围及待人工确认事项；不能完成的检查如实说明。

## 安全与 Git

- 本仓库可能公开。提交或推送前检查合同、人员、成本、供应商、接口、账号和令牌等敏感信息；参考资料或工作簿不能因为已在目录中就视为可公开。
- 不提交运行数据库、密钥、缓存、临时文件和机器专属配置；按 `.gitignore` 排除，必要时先修订忽略规则。
- 未获明确要求时不提交、推送、改写历史、清理归档或对外发布。即使获授权，也先检查准确目标与影响；不得用强制推送、重置或清理掩盖冲突。
